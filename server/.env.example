# Port this proxy server listens on
PORT=3000

# URL of the LanguageTool instance this server forwards requests to
LANGUAGETOOL_URL=http://localhost:8081

# Comma-separated allowed origins, or * to allow all
# Use * for local development; restrict in production
CORS_ORIGIN=*
